import React from 'react';
import PropTypes from 'prop-types';

const PlaceDetail = (props) => {
    /*
    const placeAddress = props.addressComponents.map((each) => { 
       // const eachLine = each.
       const addressComponent = each.long_name;    
        return <li>{addressComponent}</li>
    })
    return(
        <ul className="col-md-4 list-group">
          { placeAddress }
        </ul>
    );*/

}
export default PlaceDetail;